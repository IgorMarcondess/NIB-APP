import { View } from "react-native";

type datasProps = {
    date: number;
    mes: string;
}

export const data = (Props: datasProps) => {
    const {date, mes} = Props;
    <View className="">

    </View>
}